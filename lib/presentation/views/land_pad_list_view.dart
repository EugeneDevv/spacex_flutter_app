import 'package:flutter/material.dart';

class LandPadListView extends StatelessWidget {
  const LandPadListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('LandPadListView'),
    );
  }
}
