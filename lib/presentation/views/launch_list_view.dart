import 'package:flutter/material.dart';

class LaunchListView extends StatelessWidget {
  const LaunchListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('LaunchListView'),
    );
  }
}
