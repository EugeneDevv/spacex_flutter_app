import 'package:flutter/material.dart';

class RocketListView extends StatelessWidget {
  const RocketListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('RocketListView'),
    );
  }
}
