import 'package:flutter/material.dart';

class LaunchListScreen extends StatelessWidget {
  const LaunchListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('LaunchListScreen'),
    );
  }
}