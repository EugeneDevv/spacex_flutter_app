import 'package:flutter/material.dart';

class LandPadListScreen extends StatelessWidget {
  const LandPadListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('LandPadListScreen'),
    );
  }
}