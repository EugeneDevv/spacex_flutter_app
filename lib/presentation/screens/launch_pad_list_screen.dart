import 'package:flutter/material.dart';

class LaunchPadListScreen extends StatelessWidget {
  const LaunchPadListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('LaunchPadListScreen'),
    );
  }
}