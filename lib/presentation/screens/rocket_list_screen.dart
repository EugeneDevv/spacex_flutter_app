import 'package:flutter/material.dart';

class RocketListScreen extends StatelessWidget {
  const RocketListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('RocketListScreen'),
    );
  }
}